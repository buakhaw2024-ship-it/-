# 全谱系博弈训练系统 v3 — 正式版 ✅

把 v2.0 的单文件巨石（`game_trainer.html`）重构为**模块化、事件驱动、AI 真正可博弈**的训练系统，同时保留"单文件离线分发"的核心优势。

> **v3 已完整实现（Phase 1-4 全部通过验收）**。分发文件：`dist/game_trainer.html`（119 KB，44 模块）。

---

## 📁 目录

| 文件 | 作用 |
|------|------|
| `00-源码-v2.0.html` | 当前系统源代码（83KB），改造起点，只读参考 |
| `01-PRD.md` | 产品需求文档 —— 做什么 + 为什么 |
| `02-技术架构.md` | 技术架构 —— 6 个 JS 模块目录 + 事件总线设计 |
| `03-实施路线图.md` | **执行入口** —— 4 个 Phase + 任务清单 + 验收 |
| `04-AI对手决策引擎设计.md` | AI 引擎详细设计 —— 6 对手差异化逻辑 + 跨回合记忆 |
| `README.md` | 本文件 —— 使用说明 |

---

## 🚀 给 Claude Code 的执行指令

> **"读 README.md，按 03-实施路线图.md 从 Phase 1 开始执行，源码在 00-源码-v2.0.html"**

执行约定：
1. 严格按 `03-实施路线图.md` 的 Phase 顺序推进，逐条勾选任务。
2. 每个 Phase 末，用该 Phase 的**验收清单**自检并汇报结果。
3. 每个 Phase 末 `git commit` 并 push 到分支 `claude/tender-rubin-AkDWU`。
4. **单文件分发能力（`build.js`）在每个 Phase 都必须保持可用**——任何阶段都能 `node build.js` 产出可双击运行的 `dist/game_trainer.html`。
5. 严守架构约束：`ui` 不直连 `engine`/`scenarios` 逻辑，`engine` 不碰 DOM，模块间通过事件总线通信。

---

## 🏗️ 架构速览

```
src/
├── core/       ① 事件总线 + 全局状态 + 启动
├── data/       ② 静态数据（对手/场景/宗师/策略）
├── engine/     ③ AI 决策引擎（6对手差异化 + 跨回合记忆 + 情绪状态机）
├── scenarios/  ④ 7 大博弈场景（统一接口 + 注册表）
├── ui/         ⑤ 视图层（只渲染，不含业务逻辑）
└── analytics/  ⑥ 心理画像 + 复盘评分 + 持久化

build.js  →  dist/game_trainer.html （单文件，可离线分发）
```

数据流：`UI 操作 → emit 事件 → engine/scenario 处理 → 更新 store → emit 状态变更 → UI 重渲染`

---

## 🎯 v3 三大支柱
1. **架构模块化** —— 6 模块 + 事件总线，开发态多文件，分发态一键单文件。
2. **AI 智能化** —— 对手有记忆、有情绪、有破绽，形成真实博弈训练闭环。
3. **复盘深度化** —— 逐回合评分 + 改进建议 + 真实心理画像。

---

## ▶️ 运行方式

- **开发态**：本地起 http server（ES Module 需 http 协议），打开 `src/index.html`。
  ```bash
  cd src && python -m http.server 8000   # 然后浏览器访问 http://localhost:8000
  ```
- **分发态**：`node build.js` → 双击 `dist/game_trainer.html` 即可（无需服务器）。

---

## ✅ v3 Phase 4 新功能（正式版）

| 功能 | 描述 |
|------|------|
| 逐回合评分 | 每轮决策 0-100 分，颜色分级（绿/黄/红）|
| 转折点标注 | 自动定位关键失误或最佳一手 |
| 对手情绪曲线 | 内联 SVG 展示信任/愤怒随回合演化 |
| 改进建议 | 表现分析 + 针对特定对手的专项技巧 |
| 真实心理画像 | 8 维指标基于玩家真实决策序列计算 |
| 识破对手 | 每局 2 次消耗式"peek"，查看对手当前情绪状态 |

构建：`node build.js` — 测试：`node test_phase4.js`（46/46 通过）
